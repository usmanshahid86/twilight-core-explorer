import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function RewardsPage() {
  return (
    <PlaceholderPage
      title="Rewards"
      phase="12"
      description="Epochs, slot rewards, claim history, and balances — history-only until Phase 7.2."
    />
  );
}
