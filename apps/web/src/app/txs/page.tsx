import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function TxsPage() {
  return (
    <PlaceholderPage
      title="Transactions"
      phase="10b"
      description="Browse transactions with message types, status, and per-tx detail."
    />
  );
}
